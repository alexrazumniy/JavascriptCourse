// Решение Ани:

// const createTimer = (hours, minutes, seconds) => {
//     const timeLeft = `${formatTime(hours)}:${formatTime(minutes)}:${formatTime(seconds)}`;
//     // time.innerText = timeLeft;
//     console.log(timeLeft);
// }


// Добавление таймера через innerHTML

// const timerWrapper = document.createElement('div');
// timerWrapper.innerHTML = `
//     <span>${hours}</span>
//     <span>:</span>
//     <span>${minutes}</span>
//     <span>:</span>
//     <span>${seconds}</span>
//     `

// document.body.append(timerWrapper);
